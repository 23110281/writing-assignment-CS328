[build]
  publish = "report"
  # This command will build the site on Netlify
  command = "pip install -r requirements.txt && jupyter nbconvert --to html --execute --ExecutePreprocessor.kernel_name=python3 --output-dir=report analysis.ipynb --output index.html"

[build.environment]
  PYTHON_VERSION = "3.11"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    Content-Security-Policy = "frame-ancestors 'none'"
